# Phase 0 + Phase 1 实施记录

日期：2026-05-10  
分支：`feature/realtime-phase-0-1`

## 实施范围

本次按 `OpenAI实时翻译流程改造计划_20260510.md` 先完成 Phase 0 和 Phase 1。

Phase 0：

- 修复旧 `trans.html` 初始化时访问已删除 DOM 元素导致的报错。
- 保持独立项目中 Deepgram/Fish.audio 前端明文 Key 为空，不恢复旧直连凭据。
- 增加旧页面可见错误提示区域，覆盖 STT 初始化失败、翻译失败、TTS 失败和麦克风权限失败。

Phase 1：

- 在 `api-proxy` 增加 `/api/openai/realtime-translation/session`。
- `api-proxy` 同时接受 `/api-proxy/api/openai/realtime-translation/session`，方便本地 POC 页面不依赖 nginx 也能调用。
- `api-proxy` 在本地开发时直接服务 `public/`，可打开 `/realtime-translation-poc.html`。
- 后端使用 `OPENAI_API_KEY` 调 OpenAI `realtime/translations/client_secrets`。
- 增加独立 POC 页面 `public/realtime-translation-poc.html`。
- 增加浏览器模块 `public/realtime-translation.mjs`，使用 WebRTC 连接 `gpt-realtime-translate`，显示 source/target transcript，并播放译声。
- 增加 Node 内置测试，覆盖后端 payload、缺少 OpenAI Key 时的错误、旧页面静态回归、前端 POC 模块纯函数。

## 文件变更

- `api-proxy/server.js`：改为 `createApp()` 可测试结构，新增 realtime translation session 端点，health 增加 realtime 状态。
- `api-proxy/server.test.js`：新增后端测试。
- `api-proxy/package.json`：新增 `npm test`。
- `public/trans.html`：修复初始化错误，新增状态提示。
- `public/realtime-translation.mjs`：新增 WebRTC POC 模块。
- `public/realtime-translation-poc.html`：新增 POC 页面。
- `test/frontend-static.test.js`：新增旧页面和 POC 静态测试。
- `test/realtime-translation-module.test.mjs`：新增前端模块测试。
- `README.md`：补充 Phase 0 + Phase 1 状态与测试命令。

## OpenAI 官方接口依据

- `gpt-realtime-translate` 使用专用 `/v1/realtime/translations` 端点。
- 浏览器 WebRTC POC 使用后端生成的短期 client secret，不把标准 OpenAI API Key 放到浏览器。
- WebRTC SDP 连接使用 `/v1/realtime/translations/calls`。

参考：

- https://developers.openai.com/api/docs/guides/realtime-translation
- https://developers.openai.com/api/docs/guides/realtime-webrtc
- https://developers.openai.com/api/docs/models/gpt-realtime-translate

## 验证

已执行：

```powershell
cd C:\n8n\realtime-translation-demo\.worktrees\realtime-phase-0-1\api-proxy
npm test

cd C:\n8n\realtime-translation-demo\.worktrees\realtime-phase-0-1
node --test test/*.test.js test/*.test.mjs
```

下一步还需要在配置真实 `OPENAI_API_KEY` 后，用浏览器实际点击 POC 的 Start，记录 EN -> ZH 和 ZH -> EN 的首包译声延迟和字幕质量。

HTTP 验证：

- `http://127.0.0.1:3021/health` 返回 `200`，包含 `realtimeTranslation` 状态。
- `http://127.0.0.1:3021/realtime-translation-poc.html` 返回 `200`。
- 未配置 `OPENAI_API_KEY` 时，session endpoint 返回 `500` 和明确错误信息。
