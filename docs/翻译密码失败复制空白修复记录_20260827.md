# 翻译密码失败复制空白修复记录

日期：2026-08-27

## 问题

用户访问 `https://xcu.ai/trans/` 后，使用已提供的用户名和密码仍提示“用户名或密码不正确”。

## 已验证现象

- 未登录访问 `https://xcu.ai/trans/` 会显示“翻译工具访问验证”，说明保护入口正常生效。
- 使用精确用户名 `ai` 和当前访问密码登录 Cloudflare Pages 直连地址可成功进入翻译页面。
- 同一密码如果首尾带空格或换行，登录会失败。

## 根因

登录表单原先对用户名和密码做逐字比较，复制密码时附带的首尾空格、换行也会被当成密码内容。

当前访问密码包含特殊字符，用户从聊天窗口、密码管理器或手机复制时更容易带入不可见空白，从而触发失败。

## 修复

- `functions/_middleware.js`：登录校验前对用户名和密码执行首尾空白清理。
- `test/cloudflare-auth-middleware.test.mjs`：新增回归测试，覆盖复制来的用户名/密码带首尾空白仍可登录。

修复只去掉首尾空白，不改变密码中间的字符。

## 当前部署限制

尝试更新 Cloudflare Pages secret `TRANS_ACCESS_PASSWORD` 时，Wrangler 返回账号权限错误：

```text
Authentication error [code: 10000]
```

当前本机 Wrangler 登录账号是 `t@xtrigg.com`，而 Pages 项目 `trans` 属于 `trigg@gmic.ai` 对应的 Cloudflare account `bb97e6f20bab8f581c31d09b02e262a4`。

因此本次暂未重置线上访问密码。需要重新登录正确 Cloudflare 账号后，才能把访问密码换成更适合手输的纯字母数字密码。

## 验证

本地已通过：

```bash
node test/cloudflare-auth-middleware.test.mjs
node test/cloudflare-router.test.mjs
```

结果：

- 认证中间件测试：8/8 通过。
- xcu.ai 路由测试：12/12 通过。
